{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":""},{"location":"#repo-env","title":"repo-env","text":"<p>Build and operate isolated git-worktree environments across many repositories.</p> <p><code>repo-env</code> provides the <code>renv</code> CLI. An environment is a named directory containing one git worktree per selected repository. Create environments from repos matching a glob, run commands across every worktree, and open bulk pull requests \u2014 all from one command.</p>"},{"location":"#why-repo-env","title":"Why repo-env?","text":"<p>Working across many related repositories is tedious: context-switching between clones, opening the same branch in a dozen repos, forgetting to push before opening PRs. <code>repo-env</code> eliminates that friction by giving each logical task its own isolated worktree layer.</p> <ul> <li>Safe: source clones are treated as read-only; destructive operations require explicit flags.</li> <li>Fast: backed by git worktrees \u2014 no extra disk space for tracked objects.</li> <li>Scriptable: every command is composable in shell pipelines.</li> </ul>"},{"location":"#quick-navigation","title":"Quick navigation","text":"I want to\u2026 Go to\u2026 Install and get started Installation \u2192 Quick Start Follow a full workflow Workflow example Learn the commands Commands Understand layout and env resolution Concepts Fix broken worktrees / registry Troubleshooting Understand config options Configuration Contribute to the project Development Setup See what changed Changelog"},{"location":"api-reference/","title":"API Reference","text":"<p>The <code>repoenv</code> Python package is primarily an implementation detail of the <code>renv</code> CLI. Symbols and module layout may change before v1.0 \u2014 pin your dependency if you integrate programmatically.</p> <p>Direct CLI users can skip this section.</p>"},{"location":"api-reference/#repoenv_1","title":"repoenv","text":""},{"location":"api-reference/#repoenv","title":"<code>repoenv</code>","text":"<p>repo-env: git-worktree environments across many repositories.</p> <p>Public command is <code>renv</code>; import package is <code>repoenv</code>.</p>"},{"location":"api-reference/#repoenvdomain","title":"repoenv.domain","text":""},{"location":"api-reference/#repoenv.domain","title":"<code>repoenv.domain</code>","text":"<p>Pure domain layer: models and logic with no IO.</p>"},{"location":"api-reference/#repoenv.domain.Environment","title":"<code>Environment</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>A named collection of git worktrees.</p> Source code in <code>src/repoenv/domain/models.py</code> <pre><code>class Environment(BaseModel):\n    \"\"\"A named collection of git worktrees.\"\"\"\n\n    model_config = ConfigDict(extra=\"forbid\")\n\n    schema_version: int = SCHEMA_VERSION\n    name: StrictStr\n    alias: StrictStr | None = None\n    path: Path\n    source: Path\n    base_branch: StrictStr | None = None\n    created_at: datetime = Field(default_factory=_utcnow)\n    updated_at: datetime = Field(default_factory=_utcnow)\n    repos: list[RepoEntry] = Field(default_factory=list)\n\n    def touch(self) -&gt; None:\n        \"\"\"Update the ``updated_at`` timestamp in place.\"\"\"\n        self.updated_at = _utcnow()\n\n    def repo_names(self) -&gt; list[str]:\n        \"\"\"Return the repo names contained in this environment.\"\"\"\n        return [entry.repo for entry in self.repos]\n</code></pre>"},{"location":"api-reference/#repoenv.domain.Environment.touch","title":"<code>touch()</code>","text":"<p>Update the <code>updated_at</code> timestamp in place.</p> Source code in <code>src/repoenv/domain/models.py</code> <pre><code>def touch(self) -&gt; None:\n    \"\"\"Update the ``updated_at`` timestamp in place.\"\"\"\n    self.updated_at = _utcnow()\n</code></pre>"},{"location":"api-reference/#repoenv.domain.Environment.repo_names","title":"<code>repo_names()</code>","text":"<p>Return the repo names contained in this environment.</p> Source code in <code>src/repoenv/domain/models.py</code> <pre><code>def repo_names(self) -&gt; list[str]:\n    \"\"\"Return the repo names contained in this environment.\"\"\"\n    return [entry.repo for entry in self.repos]\n</code></pre>"},{"location":"api-reference/#repoenv.domain.RepoEntry","title":"<code>RepoEntry</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>A single repository's worktree within an environment.</p> Source code in <code>src/repoenv/domain/models.py</code> <pre><code>class RepoEntry(BaseModel):\n    \"\"\"A single repository's worktree within an environment.\"\"\"\n\n    model_config = ConfigDict(extra=\"forbid\")\n\n    repo: StrictStr\n    worktree_path: Path\n    remote: StrictStr = StrictStr(\"origin\")\n    base: StrictStr\n    branch: StrictStr\n    branch_created: bool = False\n    source_sha: StrictStr | None = None\n    status: RepoStatus = RepoStatus.PENDING\n    note: StrictStr | None = None\n</code></pre>"},{"location":"api-reference/#repoenv.domain.RepoStatus","title":"<code>RepoStatus</code>","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Per-repo state within an environment.</p> Source code in <code>src/repoenv/domain/models.py</code> <pre><code>class RepoStatus(str, Enum):\n    \"\"\"Per-repo state within an environment.\"\"\"\n\n    OK = \"ok\"\n    FAILED = \"failed\"\n    SKIPPED = \"skipped\"\n    STALE = \"stale\"\n    PENDING = \"pending\"\n</code></pre>"},{"location":"api-reference/#repoenv.domain.RunResult","title":"<code>RunResult</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Outcome of running a command in one repository's worktree.</p> Source code in <code>src/repoenv/domain/models.py</code> <pre><code>class RunResult(BaseModel):\n    \"\"\"Outcome of running a command in one repository's worktree.\"\"\"\n\n    model_config = ConfigDict(extra=\"forbid\")\n\n    repo: StrictStr\n    worktree_path: Path\n    exit_code: int\n    duration_s: float\n    stdout: str = \"\"\n    stderr: str = \"\"\n    skipped: bool = False\n\n    @property\n    def ok(self) -&gt; bool:\n        \"\"\"True when the command succeeded (or the repo was skipped).\"\"\"\n        return self.skipped or self.exit_code == 0\n</code></pre>"},{"location":"api-reference/#repoenv.domain.RunResult.ok","title":"<code>ok</code>  <code>property</code>","text":"<p>True when the command succeeded (or the repo was skipped).</p>"},{"location":"api-reference/#repoenv.domain.SetOp","title":"<code>SetOp</code>","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Set operations for combining environments in <code>merge</code>.</p> Source code in <code>src/repoenv/domain/selection.py</code> <pre><code>class SetOp(str, Enum):\n    \"\"\"Set operations for combining environments in ``merge``.\"\"\"\n\n    UNION = \"union\"\n    INTERSECT = \"intersect\"\n    DIFFERENCE = \"difference\"\n</code></pre>"},{"location":"api-reference/#repoenv.domain.RunSummary","title":"<code>RunSummary</code>  <code>dataclass</code>","text":"<p>Aggregate counts for a batch run.</p> Source code in <code>src/repoenv/domain/summary.py</code> <pre><code>@dataclass(frozen=True)\nclass RunSummary:\n    \"\"\"Aggregate counts for a batch run.\"\"\"\n\n    total: int\n    succeeded: int\n    failed: int\n    skipped: int\n\n    @classmethod\n    def from_results(cls, results: list[RunResult]) -&gt; \"RunSummary\":\n        \"\"\"Build a summary from per-repo results.\"\"\"\n        succeeded = sum(1 for r in results if not r.skipped and r.exit_code == 0)\n        failed = sum(1 for r in results if not r.skipped and r.exit_code != 0)\n        skipped = sum(1 for r in results if r.skipped)\n        return cls(total=len(results), succeeded=succeeded, failed=failed, skipped=skipped)\n</code></pre>"},{"location":"api-reference/#repoenv.domain.RunSummary.from_results","title":"<code>from_results(results)</code>  <code>classmethod</code>","text":"<p>Build a summary from per-repo results.</p> Source code in <code>src/repoenv/domain/summary.py</code> <pre><code>@classmethod\ndef from_results(cls, results: list[RunResult]) -&gt; \"RunSummary\":\n    \"\"\"Build a summary from per-repo results.\"\"\"\n    succeeded = sum(1 for r in results if not r.skipped and r.exit_code == 0)\n    failed = sum(1 for r in results if not r.skipped and r.exit_code != 0)\n    skipped = sum(1 for r in results if r.skipped)\n    return cls(total=len(results), succeeded=succeeded, failed=failed, skipped=skipped)\n</code></pre>"},{"location":"api-reference/#repoenv.domain.resolve_selection","title":"<code>resolve_selection(candidates, *, include=None, exclude=None)</code>","text":"<p>Resolve a selection of names from <code>candidates</code>.</p> <ul> <li><code>include</code> globs are OR-ed; if omitted, all candidates are included.</li> <li><code>exclude</code> globs are applied after include and take precedence.</li> <li>Order is preserved from <code>candidates</code>; duplicates are removed.</li> </ul> Source code in <code>src/repoenv/domain/selection.py</code> <pre><code>def resolve_selection(\n    candidates: list[str],\n    *,\n    include: list[str] | None = None,\n    exclude: list[str] | None = None,\n) -&gt; list[str]:\n    \"\"\"Resolve a selection of names from ``candidates``.\n\n    - ``include`` globs are OR-ed; if omitted, all candidates are included.\n    - ``exclude`` globs are applied after include and take precedence.\n    - Order is preserved from ``candidates``; duplicates are removed.\n    \"\"\"\n    include = include or [\"*\"]\n    exclude = exclude or []\n\n    def _split_csv(values: list[str]) -&gt; list[str]:\n        parts: list[str] = []\n        for raw in values:\n            for item in raw.split(\",\"):\n                trimmed = item.strip()\n                if trimmed:\n                    parts.append(trimmed)\n        return parts\n\n    include = _split_csv(include)\n    exclude = _split_csv(exclude)\n\n    seen: set[str] = set()\n    result: list[str] = []\n    for name in candidates:\n        if name in seen:\n            continue\n        if not any(fnmatch(name, pat) for pat in include):\n            continue\n        if any(fnmatch(name, pat) for pat in exclude):\n            continue\n        seen.add(name)\n        result.append(name)\n    return result\n</code></pre>"},{"location":"api-reference/#repoenv.domain.set_combine","title":"<code>set_combine(left, right, op)</code>","text":"<p>Combine two ordered name lists with a set operation, preserving order.</p> <p>Order follows <code>left</code> first, then any new names from <code>right</code> (for union).</p> Source code in <code>src/repoenv/domain/selection.py</code> <pre><code>def set_combine(left: list[str], right: list[str], op: SetOp) -&gt; list[str]:\n    \"\"\"Combine two ordered name lists with a set operation, preserving order.\n\n    Order follows ``left`` first, then any new names from ``right`` (for union).\n    \"\"\"\n    right_set = set(right)\n    left_set = set(left)\n\n    if op is SetOp.UNION:\n        ordered = list(left)\n        ordered.extend(name for name in right if name not in left_set)\n        return ordered\n    if op is SetOp.INTERSECT:\n        return [name for name in left if name in right_set]\n    if op is SetOp.DIFFERENCE:\n        return [name for name in left if name not in right_set]\n    raise ValueError(f\"unknown set operation: {op!r}\")\n</code></pre>"},{"location":"api-reference/#repoenv.domain.aggregate_exit_code","title":"<code>aggregate_exit_code(results)</code>","text":"<p>Map a batch of results to a single process exit code.</p> <ul> <li>no results          -&gt; NOTHING_MATCHED</li> <li>all ok/skipped       -&gt; OK</li> <li>some ok, some failed -&gt; PARTIAL</li> <li>all failed           -&gt; GENERIC</li> </ul> Source code in <code>src/repoenv/domain/summary.py</code> <pre><code>def aggregate_exit_code(results: list[RunResult]) -&gt; ExitCode:\n    \"\"\"Map a batch of results to a single process exit code.\n\n    - no results          -&gt; NOTHING_MATCHED\n    - all ok/skipped       -&gt; OK\n    - some ok, some failed -&gt; PARTIAL\n    - all failed           -&gt; GENERIC\n    \"\"\"\n    if not results:\n        return ExitCode.NOTHING_MATCHED\n\n    summary = RunSummary.from_results(results)\n    if summary.failed == 0:\n        return ExitCode.OK\n    if summary.succeeded == 0 and summary.skipped == 0:\n        return ExitCode.GENERIC\n    return ExitCode.PARTIAL\n</code></pre>"},{"location":"api-reference/#repoenvservices","title":"repoenv.services","text":""},{"location":"api-reference/#repoenv.services","title":"<code>repoenv.services</code>","text":"<p>Orchestration services: use-cases that coordinate domain + adapters.</p>"},{"location":"api-reference/#repoenvadapters","title":"repoenv.adapters","text":""},{"location":"api-reference/#repoenv.adapters","title":"<code>repoenv.adapters</code>","text":"<p>IO-boundary adapters (mockable): config, state, and git.</p>"},{"location":"api-reference/#repoenverrors","title":"repoenv.errors","text":""},{"location":"api-reference/#repoenv.errors","title":"<code>repoenv.errors</code>","text":"<p>Typed exception hierarchy mapped to stable process exit codes.</p> <p>Exit-code contract (from the safety spec):     0  ok     1  generic error     2  usage error     3  partial failure (some repos failed)     4  nothing matched     5  config / state error</p>"},{"location":"api-reference/#repoenv.errors.ExitCode","title":"<code>ExitCode</code>","text":"<p>               Bases: <code>IntEnum</code></p> <p>Stable process exit codes. Do not renumber.</p> Source code in <code>src/repoenv/errors.py</code> <pre><code>class ExitCode(IntEnum):\n    \"\"\"Stable process exit codes. Do not renumber.\"\"\"\n\n    OK = 0\n    GENERIC = 1\n    USAGE = 2\n    PARTIAL = 3\n    NOTHING_MATCHED = 4\n    CONFIG = 5\n</code></pre>"},{"location":"api-reference/#repoenv.errors.RepoEnvError","title":"<code>RepoEnvError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Base class for all repo-env errors.</p> <p>Carries an :class:<code>ExitCode</code> plus an optional user-facing <code>hint</code> that the UI layer renders as a \"next step\" line.</p> Source code in <code>src/repoenv/errors.py</code> <pre><code>class RepoEnvError(Exception):\n    \"\"\"Base class for all repo-env errors.\n\n    Carries an :class:`ExitCode` plus an optional user-facing ``hint`` that the\n    UI layer renders as a \"next step\" line.\n    \"\"\"\n\n    exit_code: ExitCode = ExitCode.GENERIC\n\n    def __init__(self, message: str, *, hint: str | None = None) -&gt; None:\n        super().__init__(message)\n        self.message = message\n        self.hint = hint\n</code></pre>"},{"location":"api-reference/#repoenv.errors.UsageError","title":"<code>UsageError</code>","text":"<p>               Bases: <code>RepoEnvError</code></p> <p>Invalid invocation / arguments.</p> Source code in <code>src/repoenv/errors.py</code> <pre><code>class UsageError(RepoEnvError):\n    \"\"\"Invalid invocation / arguments.\"\"\"\n\n    exit_code = ExitCode.USAGE\n</code></pre>"},{"location":"api-reference/#repoenv.errors.NothingMatchedError","title":"<code>NothingMatchedError</code>","text":"<p>               Bases: <code>RepoEnvError</code></p> <p>A selector or glob matched no repositories.</p> Source code in <code>src/repoenv/errors.py</code> <pre><code>class NothingMatchedError(RepoEnvError):\n    \"\"\"A selector or glob matched no repositories.\"\"\"\n\n    exit_code = ExitCode.NOTHING_MATCHED\n</code></pre>"},{"location":"api-reference/#repoenv.errors.PartialFailureError","title":"<code>PartialFailureError</code>","text":"<p>               Bases: <code>RepoEnvError</code></p> <p>Some repositories in a batch operation failed.</p> Source code in <code>src/repoenv/errors.py</code> <pre><code>class PartialFailureError(RepoEnvError):\n    \"\"\"Some repositories in a batch operation failed.\"\"\"\n\n    exit_code = ExitCode.PARTIAL\n</code></pre>"},{"location":"api-reference/#repoenv.errors.ConfigError","title":"<code>ConfigError</code>","text":"<p>               Bases: <code>RepoEnvError</code></p> <p>Configuration or persisted-state problem.</p> Source code in <code>src/repoenv/errors.py</code> <pre><code>class ConfigError(RepoEnvError):\n    \"\"\"Configuration or persisted-state problem.\"\"\"\n\n    exit_code = ExitCode.CONFIG\n</code></pre>"},{"location":"api-reference/#repoenv.errors.GitError","title":"<code>GitError</code>","text":"<p>               Bases: <code>RepoEnvError</code></p> <p>A git subprocess failed.</p> Source code in <code>src/repoenv/errors.py</code> <pre><code>class GitError(RepoEnvError):\n    \"\"\"A git subprocess failed.\"\"\"\n\n    exit_code = ExitCode.GENERIC\n</code></pre>"},{"location":"changelog/","title":"Changelog","text":""},{"location":"changelog/#changelog_1","title":"Changelog","text":"<p>All notable changes to this project will be documented in this file.</p> <p>The format follows Keep a Changelog. Versions follow Semantic Versioning. This file is updated automatically by <code>cz bump</code> \u2014 do not edit manually except before the first tagged release.</p>"},{"location":"changelog/#unreleased","title":"Unreleased","text":""},{"location":"changelog/#010-2026-07-13","title":"0.1.0 - 2026-07-13","text":"<p>First public release of <code>repo-env</code> / the <code>renv</code> CLI.</p>"},{"location":"changelog/#added","title":"Added","text":"<ul> <li><code>renv</code> CLI for managing multi-repository git worktree environments</li> <li>Commands: <code>init</code>, <code>create</code>, <code>add</code>, <code>merge</code>, <code>ls</code>, <code>repos</code>, <code>path</code>, <code>run</code>, <code>rm</code>, <code>rename</code>, <code>sync</code>, <code>status</code>, <code>check</code>, <code>prune</code>, <code>repair</code>, <code>import</code>, <code>pr</code>, <code>sh</code>, <code>completion</code>, <code>activate</code>, <code>config</code></li> <li>Environment resolution: explicit name, environment alias, config alias, CWD-in-env, <code>REPOENV_ACTIVE</code>, persisted active env</li> <li>User config (<code>repoenv.yaml</code>) and registry (<code>registry.json</code>) under <code>REPOENV_HOME</code></li> <li>Per-environment metadata in <code>.repoenv.json</code> (includes reproduction marker)</li> <li>Branch conflict strategies: <code>--on-branch-conflict detach|move|fail</code></li> <li>Bulk PR creation via GitHub CLI (<code>gh</code>); optional <code>--push</code></li> <li>Shell completion for environment names; <code>autocorrect</code> for unknown subcommands</li> <li>Integration, performance, and multi-Python (3.10\u20133.14) test coverage</li> <li>Documentation site (MkDocs Material) with user and contributor guides</li> </ul>"},{"location":"changelog/#changed","title":"Changed","text":"<ul> <li>N/A (initial release)</li> </ul>"},{"location":"changelog/#fixed","title":"Fixed","text":"<ul> <li>N/A (initial release)</li> </ul>"},{"location":"contributor-guide/commit-convention/","title":"Commit Convention","text":"<p><code>repo-env</code> uses Conventional Commits 1.0.</p>"},{"location":"contributor-guide/commit-convention/#why","title":"Why","text":"<ul> <li>Enables automatic version bumping (<code>cz bump</code> infers patch / minor / major from commit types).</li> <li>Generates a structured changelog (<code>cz changelog</code>).</li> <li>Makes the Git history machine- and human-readable.</li> </ul>"},{"location":"contributor-guide/commit-convention/#format","title":"Format","text":"<pre><code>&lt;type&gt;(&lt;scope&gt;): &lt;short summary&gt;\n\n[optional body]\n\n[optional footer(s)]\n</code></pre>"},{"location":"contributor-guide/commit-convention/#types","title":"Types","text":"Type Triggers Use for <code>feat</code> minor bump New user-visible feature <code>fix</code> patch bump Bug fix <code>docs</code> patch bump Documentation only <code>refactor</code> patch bump Code restructure, no behaviour change <code>perf</code> patch bump Performance improvement <code>test</code> patch bump Adding / fixing tests <code>build</code> patch bump Build system, deps <code>ci</code> patch bump CI/CD changes <code>chore</code> patch bump Maintenance (e.g. version bumps, tool configs) <code>revert</code> patch bump Reverts a previous commit"},{"location":"contributor-guide/commit-convention/#breaking-changes","title":"Breaking changes","text":"<p>Append <code>!</code> after the type, or add <code>BREAKING CHANGE:</code> in the footer:</p> <pre><code>feat!: remove --legacy flag\n</code></pre> <pre><code>feat(cli): redesign pr command\n\nBREAKING CHANGE: `--push` is now opt-in; branches are not pushed automatically\n</code></pre> <p>Breaking changes trigger a major version bump.</p>"},{"location":"contributor-guide/commit-convention/#scopes-optional-but-encouraged","title":"Scopes (optional but encouraged)","text":"<p>Use the package sub-directory or domain concept as scope:</p> <pre><code>feat(cli): add --parallel flag to run command\nfix(adapters/git): handle detached HEAD state\ndocs(contributor-guide): add release process\n</code></pre>"},{"location":"contributor-guide/commit-convention/#summary-rules","title":"Summary rules","text":"<ul> <li>Lowercase, no period at the end.</li> <li>Imperative mood: \"add X\" not \"adds X\" or \"added X\".</li> <li>\u2264 72 characters.</li> </ul>"},{"location":"contributor-guide/commit-convention/#enforcement","title":"Enforcement","text":""},{"location":"contributor-guide/commit-convention/#pr-title-primary-gate-squash-merges","title":"PR title (primary gate \u2014 squash merges)","text":"<p>Because the repo uses squash merges, the PR title becomes the commit on <code>main</code>. The CI job <code>Lint PR title</code> (<code>amannn/action-semantic-pull-request</code>) rejects PRs whose titles do not follow Conventional Commits.</p>"},{"location":"contributor-guide/commit-convention/#local-commit-msg-hook-contributor-helper","title":"Local commit-msg hook (contributor helper)","text":"<p>The <code>commitizen</code> <code>commit-msg</code> pre-commit hook validates each local commit:</p> <pre><code># Installed automatically by: pre-commit install --hook-type commit-msg\ngit commit -m \"oops bad message\"   # \u2192 rejected with helpful error\ngit commit -m \"fix(cli): handle empty source dir\"  # \u2192 accepted\n</code></pre>"},{"location":"contributor-guide/commit-convention/#interactive-commit-helper","title":"Interactive commit helper","text":"<pre><code>cz commit   # guided prompt that writes a valid message for you\n</code></pre>"},{"location":"contributor-guide/commit-convention/#examples","title":"Examples","text":"<pre><code>feat(cli): add --glob filter to renv create\nfix(adapters/git): avoid crash when worktree already exists\ndocs: add configuration reference page\nci: add multi-python test matrix (3.12/3.13/3.14)\nchore: bump commitizen to 3.29\nfeat!: require explicit --source flag; drop positional arg\n</code></pre>"},{"location":"contributor-guide/dev-setup/","title":"Development Setup","text":""},{"location":"contributor-guide/dev-setup/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10+ (3.12 recommended for local dev; nox installs other versions on demand via uv)</li> <li>uv \u2014 used by nox as the venv backend</li> <li>nox \u2014 task runner for all quality gates</li> <li>Git 2.38+</li> </ul>"},{"location":"contributor-guide/dev-setup/#install-nox-and-uv","title":"Install nox and uv","text":"<pre><code>pip install uv\nuv tool install nox\n</code></pre> <p>Or with pipx:</p> <pre><code>pipx install nox\npipx install uv\n</code></pre>"},{"location":"contributor-guide/dev-setup/#clone-and-set-up","title":"Clone and set up","text":"<pre><code>git clone https://github.com/ditschi/repo-env.git\ncd repo-env\nnox -s install_dev   # installs all dev extras into the active venv\n</code></pre>"},{"location":"contributor-guide/dev-setup/#run-quality-gates","title":"Run quality gates","text":"<pre><code>nox                  # default sessions: syntax, tests (3.12/3.13/3.14), lint, types, imports, dead code, dupes, cycles\nnox -s tests         # unit tests, all Python versions\nnox -s tests-3.12    # unit tests, specific Python version\nnox -s integration   # integration tests (real temp git repos, mocked gh)\nnox -s lint          # ruff + black + flake8 + isort\nnox -s check_types   # mypy\nnox -s docs          # build docs strict (fails on any warning)\nnox -s docs_serve    # live-reload local docs at http://127.0.0.1:8000\n</code></pre> <p>CI runs integration tests in a separate job on Python 3.12 only (see Quality Gates).</p>"},{"location":"contributor-guide/dev-setup/#install-pre-commit-hooks","title":"Install pre-commit hooks","text":"<pre><code>pre-commit install --hook-type commit-msg --hook-type pre-commit --hook-type pre-push\n</code></pre> <p>This installs:</p> <ul> <li><code>pre-commit</code>: formatting + lint on every commit</li> <li><code>commit-msg</code>: validates your commit message follows Conventional Commits</li> <li><code>pre-push</code>: runs mypy</li> </ul>"},{"location":"contributor-guide/dev-setup/#project-structure","title":"Project structure","text":"<pre><code>src/repoenv/       Core package\n  adapters/        External system adapters (git, gh, filesystem)\n  cli/             Typer command definitions\n  domain/          Pure domain models + logic\n  services/        Orchestration between domain + adapters\n  ui/              Rich terminal output helpers\ntests/\n  unit/            Fast, no I/O\n  integration/     Real temp git repos, mocked gh\n  performance/     CLI cold-start latency budgets (help + bash completion)\ndocs/              This site\n.github/workflows/ CI/CD pipelines\n</code></pre>"},{"location":"contributor-guide/dev-setup/#code-style","title":"Code style","text":"<ul> <li>Line length: 110</li> <li>Formatter: black (primary) + isort (import order) + ruff --fix (auto-fixable lint)</li> <li>Type hints required on all public interfaces (enforced by mypy strict)</li> <li>Docstrings: Google style (enforced by mkdocstrings rendering)</li> </ul> <p>See Quality Gates for the full list of tools.</p>"},{"location":"contributor-guide/quality-gates/","title":"Quality Gates","text":"<p>All gates run via <code>nox</code>. They also run in CI on every PR and push to <code>main</code>.</p>"},{"location":"contributor-guide/quality-gates/#ci-jobs","title":"CI jobs","text":"Job Workflow(s) Nox session(s) Python Quality gates PR, main <code>check_syntax</code>, <code>lint</code>, <code>check_types</code>, <code>necessary_imports</code>, <code>dead_code</code>, <code>duplicates</code>, <code>cyclic_imports</code> 3.12 Unit tests PR, main <code>tests</code> 3.12, 3.13, 3.14 (matrix) Integration tests PR, main <code>integration</code> 3.12 Performance budgets PR <code>performance</code> 3.12 Docs build PR <code>docs</code> 3.12 Docs deploy main \u2014 3.12 <p>On <code>main</code>, the quality job runs <code>nox</code> with default sessions (same gates as above, minus <code>format</code>).</p>"},{"location":"contributor-guide/quality-gates/#why-integration-runs-on-one-python-version","title":"Why integration runs on one Python version","text":"<p>Unit tests run on a multi-Python matrix because they exercise language-level and library compatibility across supported versions.</p> <p>Integration tests are slower (real temp git repos, subprocess CLI invocations) and focus on end-to-end workflows \u2014 git worktrees, CLI commands, mocked <code>gh</code>. Python-version differences are already covered by the unit matrix; repeating integration on 3.13/3.14 would mostly add CI time without catching new failure modes.</p> <p>CI uses 3.12 for integration \u2014 the same primary version as lint, types, and docs.</p>"},{"location":"contributor-guide/quality-gates/#gate-summary","title":"Gate summary","text":"Session Tool(s) Python What it checks <code>check_syntax</code> <code>compileall</code> 3.12 Byte-compile; catches syntax errors fast <code>tests</code> pytest + pytest-cov + pytest-xdist 3.12, 3.13, 3.14 Unit tests with coverage <code>integration</code> pytest 3.12 Real temp git repos + mocked gh <code>performance</code> pytest 3.12 CLI cold-start latency budgets (help + bash completion) <code>lint</code> ruff + black + flake8 + isort 3.12 Style + lint <code>check_types</code> mypy 3.12 Static type checking <code>necessary_imports</code> fawltydeps 3.12 Undeclared / unused dependencies <code>dead_code</code> vulture 3.12 Unused code <code>duplicates</code> pylint R0801 3.12 Duplicate code blocks <code>cyclic_imports</code> pylint R0401 3.12 Circular import detection <code>docs</code> mkdocs build --strict 3.12 Docs build with zero warnings"},{"location":"contributor-guide/quality-gates/#coverage","title":"Coverage","text":"<p>Coverage is reported on <code>tests</code> session only. The project targets \u2265 80% coverage on <code>src/repoenv</code>. PRs must not reduce coverage.</p>"},{"location":"contributor-guide/quality-gates/#type-strictness","title":"Type strictness","text":"<p>mypy is configured in <code>mypy.ini</code>. Key settings:</p> <ul> <li><code>strict = True</code> on <code>src/repoenv</code></li> <li>Integration and test files: relaxed (no strict)</li> </ul>"},{"location":"contributor-guide/quality-gates/#line-length","title":"Line length","text":"<p>110 characters. Configured uniformly in <code>pyproject.toml</code> (black, isort), <code>ruff.toml</code>, and <code>.flake8</code>.</p>"},{"location":"contributor-guide/quality-gates/#running-a-single-gate","title":"Running a single gate","text":"<pre><code>nox -s lint\nnox -s check_types\nnox -s tests-3.13   # specific Python version\n</code></pre>"},{"location":"contributor-guide/quality-gates/#running-only-new-tests-fast-feedback","title":"Running only new tests (fast feedback)","text":"<pre><code>nox -s tests -- tests/unit/domain/\nnox -s integration -- tests/integration/cli/\n</code></pre>"},{"location":"contributor-guide/quality-gates/#bypassing-formatting-for-a-line","title":"Bypassing formatting for a line","text":"<pre><code>some_long_line = value  # noqa: E501  # fmt: skip\n</code></pre> <p>Use sparingly. Prefer refactoring over bypasses.</p>"},{"location":"contributor-guide/release-process/","title":"Release Process","text":"<p>Releases are human-triggered: a maintainer runs <code>cz bump</code> locally, which computes the new version from Conventional Commits, updates <code>CHANGELOG.md</code>, commits, and creates a signed tag. Pushing the tag fires the <code>release.yml</code> workflow, which publishes to PyPI and deploys versioned docs.</p>"},{"location":"contributor-guide/release-process/#one-time-infrastructure-setup","title":"One-time infrastructure setup","text":"<p>Complete these steps once before the first <code>v0.1.0</code> tag.</p>"},{"location":"contributor-guide/release-process/#1-pypi-trusted-publishing-oidc","title":"1. PyPI Trusted Publishing (OIDC)","text":"<p>On pypi.org \u2192 Your projects \u2192 <code>repo-env</code> \u2192 Publishing \u2192 Add a new pending publisher (or configure after first upload):</p> Field Value PyPI project name <code>repo-env</code> Owner <code>ditschi</code> Repository name <code>repo-env</code> Workflow name <code>release.yml</code> Environment name <code>pypi</code> <p>Repeat on test.pypi.org with environment name <code>testpypi</code>.</p> <p>No long-lived API tokens are stored in GitHub secrets \u2014 the workflow uses OIDC (<code>id-token: write</code>).</p>"},{"location":"contributor-guide/release-process/#2-github-environments","title":"2. GitHub Environments","text":"<p>In the GitHub repo \u2192 Settings \u2192 Environments, create:</p> Environment Purpose Suggested protection <code>testpypi</code> TestPyPI publish + install smoke test Optional reviewers <code>pypi</code> Production PyPI publish Required reviewers recommended <p>Environment names must match <code>release.yml</code> (<code>environment: name: testpypi</code> / <code>pypi</code>).</p>"},{"location":"contributor-guide/release-process/#3-github-pages","title":"3. GitHub Pages","text":"<p>Ensure GitHub Pages is enabled for the repo (deploy source: GitHub Actions / <code>gh-pages</code> branch from workflows).</p>"},{"location":"contributor-guide/release-process/#4-verify-locally","title":"4. Verify locally","text":"<pre><code>nox -s install_dev\nnox -s docs\nnox -s integration\ncz bump --dry-run\n</code></pre>"},{"location":"contributor-guide/release-process/#prerequisites-every-release","title":"Prerequisites (every release)","text":"<p>Install dev extras (includes <code>commitizen</code> and <code>hatch-vcs</code>):</p> <pre><code>nox -s install_dev\n</code></pre>"},{"location":"contributor-guide/release-process/#step-by-step","title":"Step-by-step","text":""},{"location":"contributor-guide/release-process/#1-ensure-main-is-clean-and-green","title":"1. Ensure <code>main</code> is clean and green","text":"<pre><code>git checkout main &amp;&amp; git pull\n# Verify CI is green before proceeding\n</code></pre>"},{"location":"contributor-guide/release-process/#2-dry-run-bump-see-what-would-change","title":"2. Dry-run bump (see what would change)","text":"<pre><code>cz bump --dry-run\n</code></pre> <p><code>cz</code> inspects all commits since the last tag and prints the computed next version (<code>patch</code> / <code>minor</code> / <code>major</code>) based on commit types.</p>"},{"location":"contributor-guide/release-process/#3-run-the-bump","title":"3. Run the bump","text":"<pre><code>cz bump\n</code></pre> <p>This:</p> <ol> <li>Computes the next SemVer version.</li> <li>Updates <code>CHANGELOG.md</code>.</li> <li>Commits with message <code>chore(release): bump version to X.Y.Z</code>.</li> <li>Creates an annotated git tag <code>vX.Y.Z</code>.</li> </ol> <p>For the first release when history predates Conventional Commits, ensure <code>CHANGELOG.md</code> has a sensible <code>[0.1.0]</code> section before bumping, or use <code>cz bump --increment MINOR</code> after reviewing <code>cz bump --dry-run</code>.</p>"},{"location":"contributor-guide/release-process/#4-push-the-commit-and-tag","title":"4. Push the commit and tag","text":"<pre><code>git push origin main --follow-tags\n</code></pre>"},{"location":"contributor-guide/release-process/#5-ci-takes-over","title":"5. CI takes over","text":"<p>The <code>release.yml</code> workflow fires on <code>vX.Y.Z</code> tags and:</p> <ol> <li>Builds sdist + wheel with <code>hatch build</code>.</li> <li>Publishes to TestPyPI and verifies install (<code>uv tool install</code> from TestPyPI).</li> <li>Publishes to PyPI via OIDC Trusted Publishing.</li> <li>Creates a GitHub Release with changelog notes for this version.</li> <li>Deploys versioned docs: <code>mike deploy X.Y.Z stable --update-aliases</code>.</li> </ol> <p>Every release runs TestPyPI first, then PyPI \u2014 there is no separate gate flag in the workflow.</p>"},{"location":"contributor-guide/release-process/#6-verify","title":"6. Verify","text":"<pre><code>uvx repo-env@X.Y.Z --version   # install from PyPI and check\n</code></pre>"},{"location":"contributor-guide/release-process/#patch-vs-minor-vs-major","title":"Patch vs minor vs major","text":"Commit type(s) since last tag Resulting bump <code>fix</code>, <code>docs</code>, <code>refactor</code>, <code>test</code>, <code>build</code>, <code>ci</code>, <code>chore</code>, <code>perf</code>, <code>revert</code> patch (0.0.x) <code>feat</code> minor (0.x.0) <code>feat!</code> or <code>BREAKING CHANGE:</code> footer major (x.0.0)"},{"location":"contributor-guide/release-process/#changelog","title":"Changelog","text":"<p><code>CHANGELOG.md</code> at the repo root is auto-generated by <code>cz changelog</code> and updated on every <code>cz bump</code>. It follows the Keep a Changelog format.</p> <p>To preview the changelog for unreleased commits:</p> <pre><code>cz changelog --unreleased-version HEAD --dry-run\n</code></pre>"},{"location":"contributor-guide/release-process/#versioned-docs","title":"Versioned docs","text":"<p><code>mike</code> deploys each release as <code>X.Y.Z</code> and keeps a <code>stable</code> alias pointing to the latest stable release. The version selector dropdown in the docs site shows all deployed versions.</p> <p>To list deployed versions:</p> <pre><code>mike list\n</code></pre>"},{"location":"contributor-guide/release-process/#hotfix-releases","title":"Hotfix releases","text":"<p>For a hotfix on a previous minor:</p> <pre><code>git checkout -b hotfix/v1.2.x vX.Y.Z   # branch from the tag\n# cherry-pick or apply fix commits\ncz bump --increment patch\ngit push origin hotfix/v1.2.x --follow-tags\n# Open a PR from hotfix/v1.2.x -&gt; main for the cherry-pick too\n</code></pre>"},{"location":"user-guide/commands/","title":"Commands","text":"<p>All commands are invoked as <code>renv &lt;command&gt; [OPTIONS] [ARGS]</code>.</p> <p>Run <code>renv --help</code> or <code>renv &lt;command&gt; --help</code> for the latest flags.</p>"},{"location":"user-guide/commands/#renv-init","title":"<code>renv init</code>","text":"<p>First-run setup wizard. Writes the user config file.</p> <pre><code>renv init [OPTIONS]\n</code></pre> <p>Key options:</p> <ul> <li><code>--source/-s DIR</code>: default source directory of clones</li> <li><code>--dest/-d DIR</code>: default destination root for environments</li> <li><code>--default-branch BRANCH</code>: fallback default branch</li> <li><code>--install-completion</code>: record completion preference</li> <li><code>--yes/-y</code>: non-interactive</li> </ul>"},{"location":"user-guide/commands/#renv-create","title":"<code>renv create</code>","text":"<p>Create a new environment from repositories in a source directory.</p> <pre><code>renv create NAME [OPTIONS]\n</code></pre> <p><code>renv new</code> remains available as a compatibility alias.</p> <p>Highlights:</p> <ul> <li><code>--source/-s DIR</code>, <code>--dest/-d DIR</code></li> <li><code>--include/-i GLOB</code> / <code>--exclude/-x GLOB</code> (repeatable; CSV also supported)</li> <li><code>--branch/-b BRANCH</code> (create branch in each repo)</li> <li><code>--default-branch/-B BRANCH</code> (fallback when auto-detect fails)</li> <li><code>--preserve</code> (skip fetch/update; use source repos as-is)</li> <li><code>--activate</code> (set as default environment)</li> <li><code>--on-branch-conflict detach|move|fail</code> (when branch already checked out elsewhere)</li> <li><code>--dry-run/-n</code></li> </ul>"},{"location":"user-guide/commands/#directory-layout","title":"Directory layout","text":"<p>Each environment is a directory under your <code>--dest</code> root:</p> <pre><code>&lt;dest&gt;/&lt;env-name&gt;/\n  .repoenv.json          # environment metadata (also the renv-root marker)\n  &lt;repo-a&gt;/              # git worktree for repo-a\n  &lt;repo-b&gt;/              # git worktree for repo-b\n</code></pre> <p>The env directory is a container; each repository gets its own subdirectory worktree.</p>"},{"location":"user-guide/commands/#renv-ls","title":"<code>renv ls</code>","text":"<p>List all environments and their worktree status.</p> <pre><code>renv ls [--json]\n</code></pre>"},{"location":"user-guide/commands/#renv-activate","title":"<code>renv activate</code>","text":"<p>Set the default active environment for future commands (when not inside an env directory).</p> <pre><code>renv activate NAME\n</code></pre>"},{"location":"user-guide/commands/#renv-config","title":"<code>renv config</code>","text":"<p>Inspect and edit configuration/state.</p> <pre><code>renv config [KEY [VALUE]] [--unset] [--json]\n</code></pre> <p>Examples:</p> <ul> <li><code>renv config</code> (dump effective config + paths + active env)</li> <li><code>renv config source ~/src</code></li> <li><code>renv config autocorrect 0.5</code></li> </ul>"},{"location":"user-guide/commands/#renv-path","title":"<code>renv path</code>","text":"<p>Print the filesystem path to a named environment (suitable for <code>cd</code>).</p> <pre><code>renv path [ENV] [--repo NAME]\n</code></pre>"},{"location":"user-guide/commands/#renv-run","title":"<code>renv run</code>","text":"<p>Run an arbitrary shell command inside every worktree of an environment.</p> <pre><code>renv run [ENV] [OPTIONS] -- COMMAND [ARGS...]\n</code></pre> <p>Highlights:</p> <ul> <li><code>--jobs/-j N</code> (parallel workers)</li> <li><code>--include/-i GLOB</code> / <code>--exclude/-x GLOB</code> (subset selection; CSV supported)</li> <li><code>--shell</code> (run via shell to enable pipes/globs)</li> <li><code>--json</code></li> </ul>"},{"location":"user-guide/commands/#renv-pr","title":"<code>renv pr</code>","text":"<p>Open bulk pull requests for every worktree with unpushed commits.</p> <pre><code>renv pr [ENV] --title TITLE [OPTIONS]\n</code></pre> <p>Highlights:</p> <ul> <li><code>--include/-i GLOB</code> / <code>--exclude/-x GLOB</code> (subset selection; CSV supported)</li> <li><code>--push</code> (push branches before creating PRs)</li> <li><code>--skip-no-diff</code> (skip repos with no commits vs base)</li> <li><code>--if-exists skip|fail</code></li> </ul> <p>No auto-push</p> <p><code>renv pr</code> never pushes unless <code>--push</code> is given.</p>"},{"location":"user-guide/commands/#renv-repair","title":"<code>renv repair</code>","text":"<p>Recreate worktrees that are missing or marked failed/stale, using metadata stored in the registry.</p> <pre><code>renv repair [ENV] [OPTIONS]\n</code></pre> <p>Highlights:</p> <ul> <li><code>--include/-i GLOB</code> / <code>--exclude/-x GLOB</code> (subset selection; CSV supported)</li> <li><code>--on-branch-conflict detach|move|fail</code></li> <li><code>--preserve</code> (skip fetch/update)</li> <li><code>--dry-run/-n</code></li> </ul> <p>Use after manual deletion of individual worktree directories, or when <code>renv create</code>/<code>add</code> left some repos in a failed state.</p>"},{"location":"user-guide/commands/#renv-rm","title":"<code>renv rm</code>","text":"<p>Remove an environment (deletes worktrees, leaves source clones).</p> <pre><code>renv rm [ENV] [--delete-files] [--force] [--dry-run]\n</code></pre> <p>By default, <code>renv rm</code> removes the environment from the registry only. Use <code>--delete-files</code> to also remove worktrees and the env directory.</p>"},{"location":"user-guide/commands/#renv-add","title":"<code>renv add</code>","text":"<p>Add repositories to an existing environment.</p> <pre><code>renv add [ENV] [OPTIONS]\n</code></pre> <p>Highlights:</p> <ul> <li><code>--source/-s DIR</code> (defaults to env source or config)</li> <li><code>--include/-i GLOB</code> / <code>--exclude/-x GLOB</code></li> <li><code>--branch/-b BRANCH</code>, <code>--on-branch-conflict detach|move|fail</code></li> <li><code>--preserve</code>, <code>--activate</code>, <code>--dry-run/-n</code></li> </ul>"},{"location":"user-guide/commands/#renv-merge","title":"<code>renv merge</code>","text":"<p>Combine two environments into a newly created environment.</p> <pre><code>renv merge NAME LEFT RIGHT [OPTIONS]\n</code></pre> <p>Highlights:</p> <ul> <li><code>--op union|intersect|difference</code> (default: <code>union</code>)</li> <li><code>--dest/-d DIR</code>, <code>--alias/-a NAME</code>, <code>--dry-run/-n</code></li> </ul>"},{"location":"user-guide/commands/#renv-repos","title":"<code>renv repos</code>","text":"<p>List all repository names across every registered environment (multi-column).</p> <pre><code>renv repos\n</code></pre>"},{"location":"user-guide/commands/#renv-rename","title":"<code>renv rename</code>","text":"<p>Rename an environment in the registry and update its metadata.</p> <pre><code>renv rename OLD NEW\n</code></pre>"},{"location":"user-guide/commands/#renv-sync","title":"<code>renv sync</code>","text":"<p>Fetch updates from each repository's remote for an environment.</p> <pre><code>renv sync [ENV]\n</code></pre>"},{"location":"user-guide/commands/#renv-status-renv-check","title":"<code>renv status</code> / <code>renv check</code>","text":"<p>Report per-repo health: present/missing worktrees and dirty state. <code>check</code> is an alias for <code>status</code>.</p> <pre><code>renv status [ENV] [--json]\n</code></pre> <p>Start here when something looks wrong \u2014 see Troubleshooting.</p>"},{"location":"user-guide/commands/#renv-prune","title":"<code>renv prune</code>","text":"<p>Run <code>git worktree prune</code> across an environment's source repositories.</p> <pre><code>renv prune [ENV]\n</code></pre> <p>Use when git remembers worktrees whose directories were removed manually.</p>"},{"location":"user-guide/commands/#renv-import","title":"<code>renv import</code>","text":"<p>Register an environment from worktrees already present on disk.</p> <pre><code>renv import DIRECTORY [OPTIONS]\n</code></pre> <p>Highlights:</p> <ul> <li><code>--name NAME</code> (default: directory basename)</li> <li><code>--source DIR</code>, <code>--alias NAME</code></li> </ul>"},{"location":"user-guide/commands/#renv-sh","title":"<code>renv sh</code>","text":"<p>Open an interactive subshell with the environment context loaded (<code>REPOENV_ACTIVE</code>, prompt marker).</p> <pre><code>renv sh [ENV]\n</code></pre>"},{"location":"user-guide/commands/#renv-completion","title":"<code>renv completion</code>","text":"<p>Print a shell completion script to stdout (for manual installation in dotfiles).</p> <pre><code>renv completion [bash|zsh|fish]\n</code></pre> <p>See Installation for setup examples.</p>"},{"location":"user-guide/commands/#optional-env-argument","title":"Optional <code>[ENV]</code> argument","text":"<p>Most commands accept an optional environment name. When omitted, resolution follows Concepts \u2192 Environment resolution. Use <code>-</code> or rely on CWD inside an environment directory.</p>"},{"location":"user-guide/concepts/","title":"Concepts","text":"<p>How <code>renv</code> organizes repositories, environments, and configuration.</p>"},{"location":"user-guide/concepts/#source-vs-destination","title":"Source vs destination","text":"Term Meaning Source Directory tree of your read-only clones (<code>--source</code> / config <code>source</code>). <code>renv create</code> and <code>renv add</code> scan here for git repos. Destination (<code>dest</code>) Root directory where environment folders are created (<code>--dest</code> / config <code>dest</code>). Environment Named folder under <code>dest</code> containing one worktree subdirectory per selected repo. <pre><code>~/src/                          # source (clones \u2014 never modified by renv)\n  service-a/\n  service-b/\n\n~/envs/                         # dest\n  web/                          # environment \"web\"\n    .repoenv.json               # metadata + renv-root marker\n    service-a/                  # git worktree\n    service-b/                  # git worktree\n</code></pre> <p>Source clones stay untouched. All day-to-day work happens in worktrees under the environment directory.</p>"},{"location":"user-guide/concepts/#registry-and-on-disk-state","title":"Registry and on-disk state","text":"<p><code>renv</code> keeps two layers of state:</p> <ol> <li><code>registry.json</code> (under <code>REPOENV_HOME</code>) \u2014 list of known environments, repo entries, and the active environment name.</li> <li><code>.repoenv.json</code> inside each environment directory \u2014 per-env metadata and an optional <code>marker</code> block with the <code>renv create \u2026</code> command used to reproduce the env.</li> </ol> <p>Lock files (<code>.lock</code>) next to JSON files guard concurrent writes. They include PID/host/user diagnostics and are removed after successful writes.</p>"},{"location":"user-guide/concepts/#environment-resolution","title":"Environment resolution","text":"<p>When a command accepts an optional <code>[ENV]</code> argument, <code>renv</code> picks an environment in this order:</p> <ol> <li>Explicit name on the command line (<code>renv run web -- \u2026</code>)</li> <li>Environment alias stored on the environment (<code>renv create --alias \u2026</code>, or <code>renv import --alias \u2026</code>)</li> <li>Config alias from <code>repoenv.yaml</code> / <code>renv config aliases.&lt;name&gt; \u2026</code> (maps a short name to an environment name)</li> <li>Current directory \u2014 if CWD is inside an environment path (wins over the active env; a hint is printed when they differ)</li> <li><code>REPOENV_ACTIVE</code> \u2014 set by <code>renv sh</code> for subshell context</li> <li>Active environment \u2014 set by <code>renv activate</code> or <code>renv create --activate</code></li> </ol> <p>If none match, the command fails with a hint to pass a name, <code>cd</code> into an env, or run <code>renv activate</code>.</p>"},{"location":"user-guide/concepts/#aliases-two-kinds","title":"Aliases (two kinds)","text":"Kind Set via Example Resolves Environment alias <code>renv create --alias</code>, <code>renv import --alias</code>, <code>renv merge --alias</code> env <code>ado</code> also answers to <code>web</code> <code>renv run web</code> \u2192 env <code>ado</code> Config alias <code>repoenv.yaml</code> <code>aliases:</code> or <code>renv config aliases.web ado</code> shorthand <code>web</code> \u2192 env name <code>ado</code> same as typing the env name <p>Both are checked when resolving an environment selector. Use config aliases for stable shortcuts in scripts; use <code>--alias</code> when the alias is intrinsic to one environment.</p>"},{"location":"user-guide/concepts/#worktrees-and-branches","title":"Worktrees and branches","text":"<p>Each repo worktree is a normal git worktree linked to the source clone. With <code>--branch</code>, <code>renv</code> creates a new branch from the detected default branch. Without <code>--branch</code>, worktrees start detached at the default branch tip.</p> <p>If a local branch already exists and is unused, <code>renv</code> attaches it automatically. If the branch is checked out elsewhere, use <code>--on-branch-conflict detach|move|fail</code> (default: <code>detach</code>).</p> <p>Before each worktree creation, <code>renv</code> runs <code>git worktree prune</code> in the source repo to drop stale metadata.</p>"},{"location":"user-guide/concepts/#safety-model","title":"Safety model","text":"<ul> <li>Source clones: treated as read-only.</li> <li><code>renv rm</code>: registry-only by default; <code>--delete-files</code> removes worktrees and the env directory.</li> <li><code>renv pr</code>: never pushes unless <code>--push</code> is given; requires the GitHub CLI (<code>gh</code>).</li> </ul> <p>See Troubleshooting for recovery when disk and registry diverge.</p>"},{"location":"user-guide/configuration/","title":"Configuration","text":""},{"location":"user-guide/configuration/#config-file-location","title":"Config file location","text":"<p><code>renv</code> stores its config and registry under a single home directory.</p> <ul> <li>Default: platform config dir for app name <code>repoenv</code> (Linux: <code>~/.config/repoenv/</code>)</li> <li>Override: set <code>REPOENV_HOME</code> to relocate everything (CI, multiple profiles)</li> </ul> <p>Important files (relative to <code>REPOENV_HOME</code>):</p> <ul> <li><code>repoenv.yaml</code> \u2014 user-authored config</li> <li><code>registry.json</code> \u2014 machine-owned environment registry (includes the active environment)</li> </ul> <p>Run <code>renv init</code> to generate the file interactively, or create it manually.</p>"},{"location":"user-guide/configuration/#schema","title":"Schema","text":"<pre><code># Default source directory for `renv create` / `renv add` (overridable by flags)\nsource: ~/src\n\n# Default destination root for new environments\ndest: ~/envs\n\n# Fallback default branch when auto-detection fails\ndefault_branch: develop\n\n# Record completion preference (`renv init`; scripts are still user-managed)\ninstall_completion: false\n\n# Auto-correct unknown subcommands after N seconds (git-like behavior)\n# autocorrect: 0.5\nautocorrect: null\n\n# Optional shorthand: alias name -&gt; environment name (see Concepts)\naliases:\n  web: ado\n</code></pre>"},{"location":"user-guide/configuration/#aliases","title":"Aliases","text":"<p>Two alias mechanisms exist; see Concepts for resolution order.</p> Mechanism How to set Example Config alias YAML <code>aliases:</code> or <code>renv config aliases.web ado</code> type <code>web</code>, runs env <code>ado</code> Environment alias <code>renv create --alias</code>, <code>renv import --alias</code>, <code>renv merge --alias</code> env <code>ado</code> also known as <code>web</code> <p>Manage config aliases:</p> <pre><code>renv config aliases.web ado\nrenv config aliases.web --unset\nrenv config aliases.web          # read one alias\n</code></pre>"},{"location":"user-guide/configuration/#environment-variables","title":"Environment variables","text":"<p><code>REPOENV_HOME</code> relocates config, registry, and state:</p> <pre><code>REPOENV_HOME=/tmp/repoenv-profile renv ls\n</code></pre> <p>Inside <code>renv sh</code>, <code>REPOENV_ACTIVE</code> is set for subshell context.</p>"},{"location":"user-guide/configuration/#machine-state","title":"Machine state","text":"<p>Per-environment metadata lives in <code>.repoenv.json</code> inside the environment directory (also the renv-root marker). It may include a <code>marker</code> block with a reproducible <code>renv create \u2026</code> command.</p> <p>Lock files (<code>.lock</code>) guard concurrent JSON writes; see Troubleshooting.</p>"},{"location":"user-guide/installation/","title":"Installation","text":""},{"location":"user-guide/installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.10 or newer (3.12 recommended; tested on 3.10\u20133.14)</li> <li>Git 2.38+ (worktree support)</li> <li>Linux or macOS (POSIX-only)</li> <li>GitHub CLI (<code>gh</code>) \u2014 optional, required only for <code>renv pr</code> (install)</li> </ul>"},{"location":"user-guide/installation/#recommended-uv-tool","title":"Recommended: uv tool","text":"<pre><code>uv tool install repo-env\nrenv --help\n</code></pre>"},{"location":"user-guide/installation/#alternative-pipx","title":"Alternative: pipx","text":"<pre><code>pipx install repo-env\nrenv --help\n</code></pre>"},{"location":"user-guide/installation/#alternative-pip-virtual-environment","title":"Alternative: pip (virtual environment)","text":"<pre><code>python3 -m venv .venv &amp;&amp; source .venv/bin/activate\npip install repo-env\nrenv --help\n</code></pre>"},{"location":"user-guide/installation/#verify-installation","title":"Verify installation","text":"<pre><code>renv --version\n</code></pre>"},{"location":"user-guide/installation/#github-cli-for-renv-pr","title":"GitHub CLI (for <code>renv pr</code>)","text":"<pre><code>gh auth login\ngh auth status\n</code></pre>"},{"location":"user-guide/installation/#shell-completion","title":"Shell completion","text":"<p>Add to <code>~/.bashrc</code> / <code>~/.zshrc</code> / <code>~/.config/fish/config.fish</code>:</p> <pre><code>eval \"$(renv completion)\"          # auto-detects shell from $SHELL\n</code></pre> <p>Or specify explicitly:</p> bashzshfish <pre><code>renv --install-completion bash\n</code></pre> <pre><code>renv --install-completion zsh\n</code></pre> <pre><code>renv --install-completion fish\n</code></pre>"},{"location":"user-guide/installation/#upgrading","title":"Upgrading","text":"<pre><code>uv tool upgrade repo-env   # or: pipx upgrade repo-env\n</code></pre>"},{"location":"user-guide/installation/#next-steps","title":"Next steps","text":"<ul> <li>Quick Start</li> <li>Workflow example</li> </ul>"},{"location":"user-guide/quickstart/","title":"Quick Start","text":""},{"location":"user-guide/quickstart/#1-first-run-setup","title":"1. First-run setup","text":"<pre><code>renv init\n</code></pre> <p>The wizard asks for your source directory (where git clones live) and destination root for environments. Settings are stored in <code>~/.config/repoenv/repoenv.yaml</code> on Linux (see Configuration).</p>"},{"location":"user-guide/quickstart/#2-create-an-environment","title":"2. Create an environment","text":"<pre><code>renv create web -s ~/src -b feature/my-task --activate\n</code></pre> <p><code>renv</code> scans <code>~/src</code> for git repositories, creates a worktree on branch <code>feature/my-task</code> in each match, and registers the environment <code>web</code>. <code>--activate</code> sets it as the default for future commands.</p>"},{"location":"user-guide/quickstart/#3-list-environments","title":"3. List environments","text":"<pre><code>renv ls\n</code></pre>"},{"location":"user-guide/quickstart/#4-navigate-to-an-environment","title":"4. Navigate to an environment","text":"<pre><code>cd \"$(renv path web)\"\n</code></pre> <p>Inside an environment directory you can omit the env name on most commands.</p>"},{"location":"user-guide/quickstart/#5-run-a-command-across-all-worktrees","title":"5. Run a command across all worktrees","text":"<pre><code>renv run web -- git status\nrenv run -- make test          # same, when active or cwd is inside web\n</code></pre>"},{"location":"user-guide/quickstart/#6-open-bulk-pull-requests","title":"6. Open bulk pull requests","text":"<p>Requires GitHub CLI (<code>gh auth login</code>).</p> <pre><code>renv pr web --title \"feat: migrate X to Y\" --draft\nrenv pr web --title \"feat: migrate X to Y\" --push   # push branches first\n</code></pre> <p><code>renv pr</code> never pushes unless <code>--push</code> is given.</p>"},{"location":"user-guide/quickstart/#7-tear-down-an-environment","title":"7. Tear down an environment","text":"<pre><code>renv rm web --delete-files    # remove registry + worktrees + env dir\nrenv rm web                   # registry-only (files stay on disk)\n</code></pre> <p>Source clones under <code>~/src</code> are never deleted.</p>"},{"location":"user-guide/quickstart/#next-steps","title":"Next steps","text":"<ul> <li>Workflow example \u2014 full create \u2192 PR \u2192 rm walkthrough</li> <li>Concepts \u2014 layout and environment resolution</li> <li>Commands reference</li> <li>Configuration</li> </ul>"},{"location":"user-guide/troubleshooting/","title":"Troubleshooting","text":"<p>Common problems and the commands that fix them.</p>"},{"location":"user-guide/troubleshooting/#quick-diagnostic-flow","title":"Quick diagnostic flow","text":"<pre><code>flowchart TD\n  problem[Something wrong?] --&gt; status[\"renv status ENV\"]\n  status --&gt; missing{Missing worktrees?}\n  missing --&gt;|yes| repair[\"renv repair ENV\"]\n  missing --&gt;|no| dirty{Dirty / git errors?}\n  dirty --&gt;|worktree stale| prune[\"renv prune ENV\"]\n  dirty --&gt;|other| manual[Fix in worktree or source clone]\n  repair --&gt; still{Still broken?}\n  still --&gt;|yes| prune\n</code></pre> <p>Always start with:</p> <pre><code>renv status web          # or: renv check web  (same command)\nrenv status web --json     # machine-readable\n</code></pre>"},{"location":"user-guide/troubleshooting/#missing-worktree-directories","title":"Missing worktree directories","text":"<p>Symptom: <code>renv status</code> shows repos as missing; you deleted a subdirectory under the env by hand.</p> <p>Fix:</p> <pre><code>renv repair web\nrenv repair web --dry-run   # preview first\n</code></pre> <p><code>repair</code> recreates worktrees from registry metadata (branch, source path, etc.).</p>"},{"location":"user-guide/troubleshooting/#stale-git-worktree-metadata","title":"Stale git worktree metadata","text":"<p>Symptom: <code>git worktree add</code> fails with \u201calready exists\u201d but the folder is gone; orphaned worktree entries in the source repo.</p> <p>Fix:</p> <pre><code>renv prune web\nrenv repair web\n</code></pre> <p><code>prune</code> runs <code>git worktree prune</code> in each source repository linked to the environment.</p>"},{"location":"user-guide/troubleshooting/#environment-deleted-on-disk-but-still-in-registry","title":"Environment deleted on disk but still in registry","text":"<p>Symptom: <code>renv create web \u2026</code> says environment already exists after you removed the folder manually.</p> <p>Fix: Re-run <code>renv create</code> with the same name \u2014 stale registry entries for missing directories are reconciled automatically. Or remove explicitly:</p> <pre><code>renv rm web --delete-files    # if any files remain\nrenv rm web                   # registry-only cleanup\n</code></pre>"},{"location":"user-guide/troubleshooting/#no-environment-specified","title":"No environment specified","text":"<p>Symptom: <code>No environment specified and the current directory is not inside one.</code></p> <p>Fix (pick one):</p> <pre><code>renv activate web             # persist default\nrenv create web \u2026 --activate  # set on create\ncd \"$(renv path web)\"         # then omit ENV (CWD wins)\nrenv run web -- git status    # explicit name\n</code></pre>"},{"location":"user-guide/troubleshooting/#lock-file-left-behind","title":"Lock file left behind","text":"<p>Symptom: <code>.repoenv.json.lock</code> or <code>registry.json.lock</code> exists after a crash.</p> <p>Lock files record PID, host, user, and timestamp while a write is in progress. After a clean exit they are removed. If a process died mid-write:</p> <ol> <li>Check the lock file JSON for <code>pid</code> / <code>user</code>.</li> <li>Confirm no <code>renv</code> process is still running.</li> <li>Remove the stale lock file manually if needed, then retry.</li> </ol>"},{"location":"user-guide/troubleshooting/#renv-pr-failures","title":"<code>renv pr</code> failures","text":"<p>Symptom: <code>The GitHub CLI 'gh' is not available.</code></p> <p>Install and authenticate GitHub CLI:</p> <pre><code>gh auth login\ngh auth status\n</code></pre> <p>Push branches yourself, or pass <code>--push</code>:</p> <pre><code>git push   # in each worktree, or:\nrenv pr web --title \"feat: \u2026\" --push\n</code></pre>"},{"location":"user-guide/troubleshooting/#branch-already-checked-out-elsewhere","title":"Branch already checked out elsewhere","text":"<p>Symptom: worktree creation fails because the branch is active in another worktree.</p> <p>Fix: use <code>--on-branch-conflict</code> on <code>create</code>, <code>add</code>, or <code>repair</code>:</p> Value Behavior <code>detach</code> (default) create a detached worktree at the branch tip <code>move</code> stash, remove the other worktree, check out the branch here <code>fail</code> abort with an error"},{"location":"user-guide/troubleshooting/#corrupt-registry","title":"Corrupt registry","text":"<p>Symptom: JSON parse or validation errors loading <code>registry.json</code>.</p> <ol> <li>Restore from backup if you have one (<code>registry.json.bak</code> may exist).</li> <li>Fix invalid entries manually (see Configuration).</li> <li>As last resort, remove the registry and re-import environments with <code>renv import</code>.</li> </ol>"},{"location":"user-guide/troubleshooting/#getting-more-help","title":"Getting more help","text":"<ul> <li><code>renv &lt;command&gt; --help</code> \u2014 authoritative flags for your installed version</li> <li>Commands reference</li> <li>Concepts</li> </ul>"},{"location":"user-guide/workflow-example/","title":"Workflow example","text":"<p>End-to-end flow: create an environment, work across repos, open PRs, tear down.</p> <p>Assumes clones live in <code>~/src</code> and environments in <code>~/envs</code>. Adjust paths to match your <code>renv init</code> answers.</p>"},{"location":"user-guide/workflow-example/#1-one-time-setup","title":"1. One-time setup","text":"<pre><code>renv init -s ~/src -d ~/envs -y\neval \"$(renv completion)\"    # optional; add to shell rc\n</code></pre>"},{"location":"user-guide/workflow-example/#2-create-and-activate","title":"2. Create and activate","text":"<pre><code>renv create web \\\n  -s ~/src \\\n  -d ~/envs \\\n  -b feature/my-task \\\n  -i \"service-*\" \\\n  --activate\n\nrenv ls\nrenv status web\n</code></pre>"},{"location":"user-guide/workflow-example/#3-work-in-the-environment","title":"3. Work in the environment","text":"<pre><code>cd \"$(renv path web)\"\n\n# Run across all worktrees (ENV optional when active or inside env dir)\nrenv run -- git status\nrenv run -- make test\nrenv run -j 4 -- git pull --ff-only\n\n# Subshell with env context loaded\nrenv sh web\n</code></pre>"},{"location":"user-guide/workflow-example/#4-add-another-repo-later","title":"4. Add another repo later","text":"<pre><code>renv add web -s ~/src -i \"new-service\" -b feature/my-task\n</code></pre>"},{"location":"user-guide/workflow-example/#5-sync-and-check-health","title":"5. Sync and check health","text":"<pre><code>renv sync web\nrenv status web\n</code></pre> <p>If a worktree folder was deleted by mistake:</p> <pre><code>renv repair web\n</code></pre>"},{"location":"user-guide/workflow-example/#6-open-pull-requests","title":"6. Open pull requests","text":"<p>Requires GitHub CLI (<code>gh auth login</code>).</p> <pre><code># Push yourself in each repo, then:\nrenv pr web --title \"feat: migrate X to Y\" --draft\n\n# Or let renv push first:\nrenv pr web --title \"feat: migrate X to Y\" --push --skip-no-diff\n</code></pre>"},{"location":"user-guide/workflow-example/#7-tear-down","title":"7. Tear down","text":"<pre><code># Remove registry entry only (worktrees stay on disk)\nrenv rm web\n\n# Remove registry + delete worktrees and env directory\nrenv rm web --delete-files --force\n</code></pre> <p>Source clones under <code>~/src</code> are never deleted by <code>renv rm</code>.</p>"},{"location":"user-guide/workflow-example/#next-steps","title":"Next steps","text":"<ul> <li>Concepts \u2014 layout, resolution, aliases</li> <li>Troubleshooting \u2014 recovery commands</li> <li>Commands \u2014 full command reference</li> </ul>"}]}